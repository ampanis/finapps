version: '2'
services:
  finhealth-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/finHealth/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=finHealth
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
